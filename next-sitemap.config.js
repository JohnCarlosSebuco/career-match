/** @type {import('next-sitemap').IConfig} */
const config = {
    siteUrl: 'https://www.roleselector.xyz', // Replace with your domain
    generateRobotsTxt: true, // Automatically generate robots.txt
    changefreq: 'daily',
    priority: 0.7,
    sitemapSize: 5000,
  };
  
  module.exports = config;
  